pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # only needed to retrain the model
pgdumplib>=3.1         # only needed to re-extract the database dump
pytest>=7.0            # only needed to run the parity suite
